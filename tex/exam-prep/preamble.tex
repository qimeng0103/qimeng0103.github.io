% ============================================
% USTC PhD Exam Prep - Preamble
% 中科大物理博士考试备考资料导言区
% ============================================

\documentclass[10pt,a4paper]{article}

% ---------- 基础包 ----------
\usepackage[margin=1.2cm, top=1.5cm, bottom=1cm]{geometry}
\usepackage{ctex}  % 中文支持
\usepackage{multicol}  % 双栏/三栏
\usepackage{enumitem}  % 列表控制
\usepackage{hyperref}  % 超链接
\usepackage{bookmark}  % 优化 PDF 书签，避免多次编译
\usepackage{tocloft}   % 美化目录格式

% ---------- 数学包 ----------
\usepackage{amsmath,amssymb,amsthm}
\usepackage{physics}
\usepackage{mathrsfs}
\usepackage{bm}  % 粗体数学符号

% ---------- 图形与颜色 ----------
\usepackage{xcolor}
\usepackage{tcolorbox}
\tcbuselibrary{skins,breakable}
\usepackage{tikz}
\usetikzlibrary{shapes,arrows,positioning,calc}

% ---------- 页面设置 ----------
\pagestyle{empty}  % 无页眉页脚，最大化空间
\setlength{\parindent}{0pt}  % 无缩进
\setlength{\parskip}{2pt}    % 紧凑段落
\setlength{\headheight}{20pt}  % 修复 fancyhdr 警告

% ---------- 双栏设置 ----------
\setlength{\columnseprule}{0.4pt}  % 栏间分隔线
\def\columnseprulecolor{\color{gray!50}}

% ============================================
% 颜色定义 - Beamer 风格
% ============================================

% 主题色
\definecolor{ustcblue}{RGB}{0,51,153}      % 科大蓝
\definecolor{USTCBLUE}{RGB}{0,51,153}      % 科大蓝（大写兼容）
\definecolor{alertred}{RGB}{220,38,38}     % 警示红
\definecolor{successgreen}{RGB}{34,139,34} % 成功绿
\definecolor{warnorange}{RGB}{255,140,0}   % 警告橙
\definecolor{royalpurple}{RGB}{120,81,169} % 皇家紫

% 柔和背景色
\definecolor{lightblue}{RGB}{230,240,255}
\definecolor{lightred}{RGB}{255,230,230}
\definecolor{lightgreen}{RGB}{230,255,230}
\definecolor{lightorange}{RGB}{255,245,230}
\definecolor{lightpurple}{RGB}{245,230,255}
\definecolor{lightyellow}{RGB}{255,255,200}

% ============================================
% tcolorbox 样式定义
% ============================================

% 1. 核心定理/模型框（蓝色主题）
\newtcolorbox{modelbox}[1][]{
    enhanced,
    colback=lightblue,
    colframe=ustcblue,
    fonttitle=\bfseries\small,
    title=#1,
    boxrule=1pt,
    arc=3pt,
    left=4pt,right=4pt,top=3pt,bottom=3pt,
    drop shadow={opacity=0.15},
    breakable
}

% 2. 解题技巧/快速识别框（红色主题）
\newtcolorbox{tipbox}[1][]{
    enhanced,
    colback=lightred,
    colframe=alertred,
    fonttitle=\bfseries\small,
    title=#1,
    boxrule=1pt,
    arc=3pt,
    left=4pt,right=4pt,top=3pt,bottom=3pt,
    drop shadow={opacity=0.15},
    breakable
}

% 3. 重要公式/二级结论框（绿色主题）
\newtcolorbox{formulabox}[1][]{
    enhanced,
    colback=lightgreen,
    colframe=successgreen,
    fonttitle=\bfseries\small,
    title=#1,
    boxrule=1pt,
    arc=3pt,
    left=4pt,right=4pt,top=3pt,bottom=3pt,
    drop shadow={opacity=0.15},
    breakable
}

% 4. 警示/易错点框（橙色主题）
\newtcolorbox{warnbox}[1][]{
    enhanced,
    colback=lightorange,
    colframe=warnorange,
    fonttitle=\bfseries\small,
    title=#1,
    boxrule=1pt,
    arc=3pt,
    left=4pt,right=4pt,top=3pt,bottom=3pt,
    drop shadow={opacity=0.15},
    breakable
}

% 5. 物理图像/概念理解框（紫色主题）
\newtcolorbox{insightbox}[1][]{
    enhanced,
    colback=lightpurple,
    colframe=royalpurple,
    fonttitle=\bfseries\small,
    title=#1,
    boxrule=1pt,
    arc=3pt,
    left=4pt,right=4pt,top=3pt,bottom=3pt,
    drop shadow={opacity=0.15},
    breakable
}

% 6. 速查/备忘框（黄色主题）
\newtcolorbox{cheatbox}[1][]{
    enhanced,
    colback=lightyellow,
    colframe=warnorange!80!black,
    fonttitle=\bfseries\small,
    title=#1,
    boxrule=1pt,
    arc=3pt,
    left=3pt,right=3pt,top=2pt,bottom=2pt,
    breakable
}

% 7. 附录推导框（灰色主题）
\newtcolorbox{derivationbox}[1][]{
    enhanced,
    colback=gray!10,
    colframe=gray!70!black,
    fonttitle=\bfseries\small,
    title=#1,
    boxrule=0.8pt,
    arc=2pt,
    left=3pt,right=3pt,top=2pt,bottom=2pt,
    breakable
}

% 8. 错题/思路短路框（深红色主题 - 警示性最强）
\definecolor{errorred}{RGB}{180,30,30}
\definecolor{lighterror}{RGB}{255,235,235}
\newtcolorbox{errorbox}[2][]{
    enhanced,
    colback=lighterror,
    colframe=errorred,
    fonttitle=\bfseries\small,
    title={\textcolor{white}{#2}},
    attach boxed title to top left={yshift=-2mm,xshift=5mm},
    boxed title style={colback=errorred,arc=2pt,boxrule=0pt},
    boxrule=1.2pt,
    arc=3pt,
    left=4pt,right=4pt,top=8pt,bottom=4pt,
    drop shadow={opacity=0.2},
    breakable,
    #1
}

% 9. 解题思路/技巧总结框（青色主题）
\definecolor{tealcolor}{RGB}{0,128,128}
\definecolor{lightteal}{RGB}{230,255,255}
\newtcolorbox{thinkbox}[1][]{
    enhanced,
    colback=lightteal,
    colframe=tealcolor,
    fonttitle=\bfseries\small,
    title=#1,
    boxrule=1pt,
    arc=3pt,
    left=4pt,right=4pt,top=3pt,bottom=3pt,
    drop shadow={opacity=0.15},
    breakable
}

% ============================================
% 快捷命令
% ============================================

% 英文术语备注
\newcommand{\en}[1]{\textcolor{ustcblue}{\textit{#1}}}

% 重要强调
\newcommand{\key}[1]{\textcolor{alertred}{\textbf{#1}}}

% 公式中的文字说明
\newcommand{\texteq}[1]{\quad\text{\small #1}}

% 快速引用
\newcommand{\quickref}[2]{\fbox{\parbox{0.9\linewidth}{\small\textbf{#1:} #2}}}

% 考试提醒标记
\newcommand{\examnote}[1]{\textcolor{warnorange}{$\triangleright$ \textbf{#1}}}

% ============================================
% 列表样式优化
% ============================================
\setlist[itemize]{
    nosep,
    leftmargin=12pt,
    label={\textcolor{ustcblue}{$\bullet$}}
}

\setlist[enumerate]{
    nosep,
    leftmargin=15pt
}

% ============================================
% 章节标题样式
% ============================================
\usepackage{titlesec}

% ---------- Part 样式（专题分隔页）----------
\titleformat{\part}
    {\normalfont\Huge\bfseries\color{ustcblue}\sffamily}
    {专题 \thepart：}{0.3em}{}
    [\vspace{-0.3em}\textcolor{ustcblue}{\rule{\linewidth}{2pt}}]
\titlespacing*{\part}{0pt}{24pt}{12pt}

\titleformat{\section}
    {\Large\bfseries\sffamily\color{ustcblue}}
    {\thesection}{0.5em}{}
    [\vspace{-0.5em}\textcolor{ustcblue}{\rule{\linewidth}{1pt}}]

\titleformat{\subsection}
    {\large\bfseries\sffamily\color{alertred}}
    {\thesubsection}{0.5em}{}

\titleformat{\subsubsection}
    {\normalsize\bfseries\sffamily\color{successgreen}}
    {\thesubsubsection}{0.5em}{}

\titlespacing*{\section}{0pt}{10pt}{5pt}
\titlespacing*{\subsection}{0pt}{8pt}{4pt}
\titlespacing*{\subsubsection}{0pt}{6pt}{3pt}

% ---------- 目录格式设置 ----------
\setcounter{tocdepth}{1}  % 目录只显示到 section 级别

\renewcommand{\cftpartfont}{\bfseries\color{ustcblue}\sffamily}
\renewcommand{\cftpartleader}{\cftdotfill{\cftdotsep}}
\renewcommand{\cftsecfont}{\small}
\renewcommand{\cftsecpagefont}{\small}
\setlength{\cftbeforepartskip}{10pt}
\setlength{\cftbeforesecskip}{4pt}
\setlength{\cftpartnumwidth}{2.5em}
\setlength{\cftsecindent}{2.5em}
\setlength{\cftsecnumwidth}{2em}

% ============================================
% 页眉设置（可选）
% ============================================
\usepackage{fancyhdr}
\pagestyle{fancy}
\fancyhf{}
\fancyhead[L]{\small\textcolor{gray}{USTC PhD Exam Prep}}
\fancyhead[R]{\small\textcolor{gray}{\nouppercase{\leftmark}}}
\renewcommand{\headrulewidth}{0.4pt}
\renewcommand{\headrule}{\hbox to\headwidth{\color{gray}\leaders\hrule height \headrulewidth\hfill}}
